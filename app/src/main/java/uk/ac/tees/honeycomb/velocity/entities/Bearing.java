package uk.ac.tees.honeycomb.velocity.entities;

/**
 * Bearing enum or usage in the getBearing method in the BusStop interface.
 * Denotes the bearings of the bus stops.
 */
public enum Bearing {
    N,
    E,
    S,
    W,
    NE,
    SE,
    SW,
    NW
}
